<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{titulo}}</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="topbar">
    <h1>Painel de Administração - Almoço Prodigi 2025</h1>
    <nav>
      <a href="/">Site público</a>
      <a href="/admin">Admin</a>
      <a href="/admin/export-csv">Exportar CSV</a>
      <a href="/admin/logout">Sair</a>
    </nav>
  </header>

  <main class="conteudo">
    <h2>Inscrições</h2>

    {{#if msg}}
      <p class="ok">{{msg}}</p>
    {{/if}}

    {{#if erro}}
      <p class="erro">{{erro}}</p>
    {{/if}}

    {{#if inscricoes.length}}
      <table class="tabela-inscritos">
        <thead>
          <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Email</th>
            <th>Telefone</th>
            <th>Distrito</th>
            <th>Concelho</th>
            <th>Menu</th>
            <th>Peixe</th>
            <th>Carne</th>
            <th>Sobremesa</th>
            <th>Estado</th>
          </tr>
        </thead>
        <tbody>
          {{#each inscricoes}}
            <tr>
              <td>{{id}}</td>
              <td>{{nome}}</td>
              <td>{{email}}</td>
              <td>{{telefone}}</td>
              <td>{{distrito}}</td>
              <td>{{concelho}}</td>
              <td>{{menu}}</td>
              <td>{{pratoPeixe}}</td>
              <td>{{pratoCarne}}</td>
              <td>{{sobremesa}}</td>
              <td>{{#if cancelado}}Cancelado{{else}}Ativo{{/if}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    {{else}}
      <p>Ainda não há inscrições.</p>
    {{/if}}

    <hr style="margin:30px 0;">

    <section>
      <h2>Resumo para o Restaurante</h2>

      <h3>Pratos de Peixe</h3>
      {{#if resumoMenus.peixe}}
        <table class="tabela-inscritos">
          <thead>
            <tr>
              <th>Prato</th>
              <th>Número de pessoas</th>
            </tr>
          </thead>
          <tbody>
            {{#each resumoMenus.peixe}}
              <tr>
                <td>{{@key}}</td>
                <td>{{this}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      {{else}}
        <p>Sem escolhas registadas.</p>
      {{/if}}

      <h3>Pratos de Carne</h3>
      {{#if resumoMenus.carne}}
        <table class="tabela-inscritos">
          <thead>
            <tr>
              <th>Prato</th>
              <th>Número de pessoas</th>
            </tr>
          </thead>
          <tbody>
            {{#each resumoMenus.carne}}
              <tr>
                <td>{{@key}}</td>
                <td>{{this}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      {{else}}
        <p>Sem escolhas registadas.</p>
      {{/if}}

      <h3>Sobremesas</h3>
      {{#if resumoMenus.sobremesa}}
        <table class="tabela-inscritos">
          <thead>
            <tr>
              <th>Sobremesa</th>
              <th>Número de pessoas</th>
            </tr>
          </thead>
          <tbody>
            {{#each resumoMenus.sobremesa}}
              <tr>
                <td>{{@key}}</td>
                <td>{{this}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      {{else}}
        <p>Sem escolhas registadas.</p>
      {{/if}}
    </section>

    <hr style="margin:30px 0;">

    <section class="secao-contacto">
      <h2>Enviar email de recordatório</h2>
      <p>
        Envia um email a todos os inscritos <strong>ativos</strong> com o número
        de dias que faltam para o almoço e o link para anular a inscrição.
      </p>

      <form method="post" action="/admin/enviar-recordatorio">
        <label for="mensagemExtra">
          Mensagem extra para incluir no email (opcional):
        </label>
        <textarea id="mensagemExtra" name="mensagemExtra" rows="4" style="width:100%;"></textarea>

        <button type="submit" class="btn-secundario" style="margin-top:10px;">
          Enviar recordatório para todos os inscritos ativos
        </button>
      </form>
    </section>
  </main>
</body>
</html>