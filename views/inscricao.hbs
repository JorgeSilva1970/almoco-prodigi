<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{titulo}}</title>
  <link rel="stylesheet" href="/css/styles.css">
  <script defer src="/js/main.js"></script>
</head>
<body>
  <header class="topbar">
    <h1>ENCONTRO DA TURMA PRODIGI - Turma 2025/2026</h1>
    <nav>
      <a href="/">Início</a>
      <a href="/onde-quando">Onde & Quando</a>
      <a href="/menus">Menus & Preços</a>
      <a href="/inscricao">Inscrição</a>
      <a href="/lista">Lista de Inscritos</a>
      <a href="/galeria">Galeria</a>
      <a href="/contacto">Contactos</a>
      <a href="/alojamento">Alojamento</a>
    </nav>
  </header>

  <main class="conteudo">
    <h2>Formulário de Inscrição</h2>

    <form method="post" action="/inscricao" class="form-inscricao">
      <label for="nome">
        Nome completo:
        <input type="text" id="nome" name="nome" required>
      </label>

      <label for="telefone">
        Telefone:
        <input type="text" id="telefone" name="telefone" required>
      </label>

      <label for="email">
        Email:
        <input type="email" id="email" name="email" required>
      </label>

      <label for="distrito">
        Distrito:
        <select id="distrito" name="distrito" required>
          <option value="">-- Seleciona o distrito --</option>
          {{#each distritos}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
        </select>
      </label>

      <label for="concelho">
        Concelho:
        <select id="concelho" name="concelho" required>
          <option value="">-- Seleciona primeiro o distrito --</option>
        </select>
      </label>

      <label for="menu">
        Tipo de menu:
        <select id="menu" name="menu" required>
          <option value="">-- Seleciona o tipo de menu --</option>
          <option value="Adulto">Adulto</option>
          <option value="Criança 4-10 anos">Criança 4-10 anos</option>
          <option value="Criança até 4 anos">Criança até 4 anos</option>
        </select>
      </label>

      <h3>Escolha do Menu</h3>

      <label for="pratoPeixe">
        Prato de Peixe (1 opção):
        <select id="pratoPeixe" name="pratoPeixe" required>
          <option value="">-- Seleciona o prato de peixe --</option>
          {{#each opcoesPeixe}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
          <option value="Não pretendo prato de peixe">Não pretendo</option>
        </select>
      </label>

      <label for="pratoCarne">
        Prato de Carne (1 opção):
        <select id="pratoCarne" name="pratoCarne" required>
          <option value="">-- Seleciona o prato de carne --</option>
          {{#each opcoesCarne}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
          <option value="Não pretendo prato de carne">Não pretendo</option>
        </select>
      </label>

      <label for="sobremesa">
        Sobremesa (1 opção):
        <select id="sobremesa" name="sobremesa" required>
          <option value="">-- Seleciona a sobremesa --</option>
          {{#each opcoesSobremesa}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
          <option value="Não pretendo sobremesa">Não pretendo</option>
        </select>
      </label>

      <button type="submit" class="btn-principal">Enviar inscrição</button>
    </form>

    <section class="secao-contacto" style="margin-top:30px;">
      <h3>Em caso de dúvida</h3>
      <p>
        Organizador: <strong>{{emailOrganizador}}</strong><br>
        Telemóvel: <strong>{{telemovelOrganizador}}</strong>
      </p>
    </section>
  </main>
</body>
</html>